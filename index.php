<?php
/**
 * SazehShop — نقطه ورود جایگزین (برای هاست‌هایی که DocumentRoot را روی ریشه پروژه می‌گذارند)
 * ---------------------------------------------------------------------------
 * روش توصیه‌شده: DocumentRoot دامنه را روی پوشه `public/` تنظیم کنید.
 * در آن حالت به این فایل نیازی نیست و نقطه ورود، فایل `public/index.php` است.
 *
 * اگر دسترسی به تنظیم DocumentRoot ندارید و کل پروژه را در `public_html` آپلود
 * کرده‌اید، دو راه دارید:
 *   ۱) اجازه بدهید فایل `.htaccess` ریشه، درخواست‌ها را به public/ هدایت کند
 *      (روی اکثر هاست‌های آپاچی همین کافی است).
 *   ۲) اگر mod_rewrite یا .htaccess غیرفعال است، همین فایل کار fallback را انجام
 *      می‌دهد: آدرس سایت (و حتی /index.php) بدون هیچ تنظیمی کار می‌کند.
 */

declare(strict_types=1);

// این فایل فقط وقتی اجرا می‌شود که درخواست مستقیماً به آن رسیده باشد؛ یعنی
// .htaccess در کار نبوده است. پس آدرس‌ها به شکل /index.php/... ساخته می‌شوند و
// فایل‌های استاتیک از پوشه public خوانده می‌شوند.
define('SHOPSTORE_FALLBACK_ENTRY', true);

require __DIR__ . '/public/index.php';
